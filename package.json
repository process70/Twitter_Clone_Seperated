{
  "name": "Twitter Clone Seperated",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./backend/index",
    "build": "npm install && npm install --prefix ./frontend && npm run build --prefix ./frontend"
  }
}